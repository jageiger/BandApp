<%= form_for @band do |f| %>
  <% if @band.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@band.errors.count, "error") %> prohibited this band from being saved:</h2>

      <ul>
      <% @band.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  <div class="field form-group">
    <%= f.label :aboutus %><br>
    <%= f.text_area :aboutus, class: "form-control" %>
  </div>
  <!--<div class="field">
    <%= f.label :schedule %><br>
    <%= f.date_select :schedule %>
  </div> -->
  <div class="actions">
    <%= f.submit class: "btn btn-default btn-sm"%>
  </div>
<% end %>
